<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hello.mybatistest.repository.MemberRepository">

    <select id="findAll" resultType="Member">
        SELECT id, name, nick_name, amount, reg_date
        FROM MEMBER
    </select>

</mapper>